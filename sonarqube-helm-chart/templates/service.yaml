apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-sonarqube
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 9000
      protocol: TCP
  selector:
    app: sonarqube
